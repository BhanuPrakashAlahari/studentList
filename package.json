{
  "name": "student-list-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontEnd",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend"
  }
}
